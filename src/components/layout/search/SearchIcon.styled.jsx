import styled from 'styled-components';

export const SSearchIcon = styled.button`
  min-width: 20px;
  height: 100%;

  &:hover {
    opacity: 0.5;
  }

  .icons {
    width: 22px;
    height: 22px;
    aspect-ratio: 1 / 1;
  }
`;
