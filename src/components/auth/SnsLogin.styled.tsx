import styled from 'styled-components';

export const SSnsIcon = styled.div`
  width: 30px;
  height: 30px;

  top: 50%;
  left: 10px;

  display: flex;
  align-items: center;
  justify-content: center;

  transform: translateY(-50%);
  position: absolute;
`;
